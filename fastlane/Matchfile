git_url("git@github.com:RISCfuture/certificates.git")
storage_mode("git")

type("appstore")
team_id("2NFSK2WB24")

# Only the distribution certificate is shared. The project signs automatically, so Xcode fetches the
# provisioning profiles itself — see the `-allowProvisioningUpdates` in the Fastfile's archive step.
app_identifier([])
skip_provisioning_profiles(true)
