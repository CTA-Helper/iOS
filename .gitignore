.DS_Store

# Xcode
build/
DerivedData/
**/xcuserdata/
*.xcuserstate

# Swift Package Manager
.build/
.swiftpm/

# SwiftLint / swift-format
.swiftlint/RemoteConfigCache
.swift-format

# Signing keys & secrets — never commit
*.p8
*.p12
*.key

# fastlane outputs — the site's captures are staged in screenshots-light/ and screenshots-dark/,
# and only the scaled copies the site lane installs under docs/ are committed.
fastlane/screenshots/
fastlane/screenshots-*/
fastlane/test_output/
fastlane/report.xml
fastlane/Preview.html

.claude/settings.local.json
.mcp.json

# fastlane secrets — fastlane loads fastlane/.env automatically
fastlane/.env
fastlane/.env.*
!fastlane/.env.example
