# frozen_string_literal: true

source "https://rubygems.org"

gem "fastlane", "~> 2.237"

# Default gems fastlane still requires but Ruby 3.4+ no longer bundles, so Bundler has to be told
# about them explicitly or every lane warns on load.
gem "abbrev", "~> 0.1.2"
gem "nkf", "~> 0.3.0"
gem "ostruct", "~> 0.6.3"
